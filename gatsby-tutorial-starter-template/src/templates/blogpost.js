import React from 'react'
import Layout from '../components/layout'

const BlogpostTemplate = ({data}) => {
    return (
        <Layout>
            <div>
                <p>This is a blogopst template for the JAMstack tutorial on Morgan Codes.</p>
            </div>
        </Layout>
    )
}

export default BlogpostTemplate